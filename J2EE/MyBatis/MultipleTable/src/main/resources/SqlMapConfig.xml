<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--引入外部properties配置文件-->
    <properties resource="jdbc.properties"/>

    <!--取别名-->
    <typeAliases>
        <typeAlias type="com.study.domain.User" alias="user"/>
        <typeAlias type="com.study.domain.Order" alias="order"/>
        <typeAlias type="com.study.domain.Role" alias="role"/>
    </typeAliases>

    <!--自定义类型处理器-->
    <typeHandlers>
        <typeHandler handler="com.study.handler.DateTypeHandler"/>
    </typeHandlers>


    <!--配置数据源环境-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <!--加载映射文件-->
    <mappers>
        <mapper resource="com/study/mapper/UserMapper.xml"/>
        <mapper resource="com/study/mapper/OrderMapper.xml"/>
    </mappers>
</configuration>